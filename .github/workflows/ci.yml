name: CI

on:
  pull_request:
  push:
    branches: [main]
  # Lets a failed run be retried without an empty commit.
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    name: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The changeset check below diffs against the base branch, and a
          # shallow clone does not contain it at any depth.
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      # Before the tests, even though `pretest` builds too, so a broken build is
      # reported as a broken build rather than as a failing test.
      - run: pnpm build

      # The probe-safety invariants are checked in a real browser, because the
      # failures they guard against are cascade and layout failures that jsdom
      # cannot have. Chromium only — nothing here is engine-specific.
      - run: pnpm exec playwright install --with-deps chromium

      # `pretest` builds first, so the browser test always runs against current
      # source rather than whatever `dist` happened to hold.
      #
      # XRAY_REQUIRE_BROWSER turns "Chromium would not launch" from a skip into a
      # failure. Locally the skip is right — a contributor who has only run
      # `pnpm install` should not get an error they cannot act on — but in CI a
      # silent skip would mean the invariants stopped being checked and nothing said
      # so.
      - run: pnpm test
        env:
          XRAY_REQUIRE_BROWSER: '1'

      # The playground is a workspace member now, so one install covers it and it
      # can be type-checked. It never was before, which is how `<shadow-card>` went
      # untyped and the plugin's own config sat behind a cast.
      - run: pnpm --filter xray-playground typecheck

      # A release needs either a changeset or a deliberate empty one. Catching
      # that on the PR is much cheaper than noticing after merge.
      - name: Changeset present
        # The release PR is the one PR that legitimately has none, because
        # versioning consumes them. Checking it there would fail every release.
        if: github.event_name == 'pull_request' && !startsWith(github.head_ref, 'changeset-release/')
        run: pnpm changeset status --since=origin/${{ github.base_ref }}

  site:
    name: site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      # The site consumes the published package like any other dependency, so it
      # no longer needs the sibling build. That is also what lets Vercel build it
      # straight from the repo with no configuration.
      # --ignore-workspace: a root pnpm-workspace.yaml governs every directory
      # beneath it, and site is deliberately not a member. Without this pnpm
      # installs the workspace instead and leaves site/node_modules empty, which
      # surfaces later as a missing binary rather than as a failed install.
      - run: pnpm install --frozen-lockfile --ignore-workspace
        working-directory: site
      # `pnpm typecheck`, not `tsc` directly: the docs collection is generated, and
      # the script is what guarantees it exists first.
      - run: pnpm typecheck
        working-directory: site
      - run: pnpm build
        working-directory: site
